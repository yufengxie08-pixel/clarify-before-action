interface:
  display_name: "先问清楚再执行"
  short_description: "明确的任务直接执行；存在关键歧义时先澄清、复述并等待确认"
  default_prompt: "Use $clarify-before-action to execute clear requests directly and clarify material ambiguity before acting."

policy:
  allow_implicit_invocation: true
