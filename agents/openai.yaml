interface:
  display_name: "先问清楚再执行"
  short_description: "在所有任务执行前先澄清歧义、复述完整理解并等待你的明确确认"
  default_prompt: "Use $clarify-before-action to clarify my request, restate your understanding, and wait for my confirmation before acting."

policy:
  allow_implicit_invocation: true
